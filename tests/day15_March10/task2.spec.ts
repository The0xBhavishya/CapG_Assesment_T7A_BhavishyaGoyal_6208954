import { expect, test } from "@playwright/test";

test("Gmail keyboard compose and send", async ({ page }) => {
   // unable to login due to some google login security issue as Gmail blocks automated login


    // await page.goto("https://mail.google.com/");
    // await page.keyboard.press("Shift+c");
    // await page.fill("textarea[name='to']", "recipient@example.com");
    // await page.fill("input[name='subjectbox']", "Test Subject");
    // await page.fill("div[aria-label='Message Body']", "Hello, this is a test email.");
    // await page.keyboard.press("Control+Enter");
    // await page.screenshot({ path: "gmail-compose.png" });
    // await page.waitForTimeout(5000);
});